# Batman AI - Backend Dependencies (runs locally, NOT in Docker)
# LLM/embedding libs (sentence-transformers, faiss) are in embedding-service Docker container

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
pydantic[email]

# Database clients (MongoDB + Redis run locally on Windows)
motor==3.7.0
pymongo==4.10.1
redis[hiredis]==5.2.1

# Auth
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# PDF processing
PyMuPDF==1.25.3
python-multipart==0.0.20

# HTTP client (calls Ollama + embedding-service Docker containers)
httpx==0.28.1

# Utilities
python-dotenv==1.0.1
